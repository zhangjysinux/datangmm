[Unit]
Description=Non-Graphic Feedback Daemon
Requires=dbus.socket
After=pre-user-session.target voipService.service

[Service]
User=service
Group=systemapps
ExecStart=/usr/bin/voipTestService
Restart=always

[Install]
WantedBy=user-session.target

